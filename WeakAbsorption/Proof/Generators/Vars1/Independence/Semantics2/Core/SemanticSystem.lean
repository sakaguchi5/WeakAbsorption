/-!
このファイルに残すべき内容:
- rule-relative semantic system の核インターフェース。
- kernel / shell / state / regime / row / shape をどう束ねるかの抽象 API。
- `Necessity` や具体的な `R4` 実装に依存しない基底定義。

ここに置いてはいけないもの:
- `R4` 固有の観測値。
- old comparison 補題。
-/
