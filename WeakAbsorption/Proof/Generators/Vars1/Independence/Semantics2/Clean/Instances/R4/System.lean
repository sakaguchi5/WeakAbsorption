import WeakAbsorption.Proof.Generators.Vars1.Independence.Semantics2.Core.Main
import WeakAbsorption.Proof.Generators.Vars1.Independence.Semantics2.Targets.Main
import WeakAbsorption.Proof.Generators.Vars1.Independence.Semantics2.Clean.README

/-!
このファイルに残すべき内容:
- `R4ObservedTarget` を入力にして作る clean-room な semantic system。
- kernel / shell / state / regime / row / shape を target ごとに返す新しい `R4System`。

ここに置いてはいけないもの:
- old `R4KernelObservedSpec` への直接依存。
- old `Necessity` の補題。
- old `PackageBridge` の比較補題。
-/
