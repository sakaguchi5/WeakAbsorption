import WeakAbsorption.Proof.Generators.Vars1.Independence.Semantics2.Clean.Instances.R4.Package

/-!
このファイルに残すべき内容:
- clean-room package から shape view を再構成する層。
- shape family や row-shape の純粋述語。

ここに置いてはいけないもの:
- old `Shape.lean` の exact catalog のコピー。
- `Necessity` 由来の shape comparison。
-/
